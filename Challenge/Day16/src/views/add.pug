H1 Add Movie Page!

legend
  form(action="/add", method="post")
    input(type="text", name="title" placeholder="Title")
    textarea(name="synopsis", cols="30", rows="10" placeholder="Synopsis")
    input(type="text", name="genre" placeholder="Separate genre by comma(,)")
    input(type="submit", value="Add")
      
